# Zależności do generowania screenshotów web UI (tools/screenshot.py).
#   pip install -r tools/requirements.txt
#   python -m playwright install chromium
#   python tools/screenshot.py
playwright>=1.40
